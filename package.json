{
  "name": "ai-template",
  "version": "1.0.0",
  "description": "Useless machine — multi-agent testing playground",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src tests",
    "format": "prettier --write src tests",
    "format:check": "prettier --check src tests",
    "check": "npm run build && npm run test && npm run lint"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/deljdlx/ai-template.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^10.0.1",
    "prettier": "^3.8.1",
    "sass": "^1.97.3",
    "vite": "^7.3.1",
    "vitest": "^4.0.18"
  }
}
